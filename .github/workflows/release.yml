# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Release

on:
  push:

#on:
#  push:
#    tags:
#      - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:
#  mac_release:
#    runs-on: macos-latest
#    steps:
#      - name: Check out a copy of the repository
#        uses: actions/checkout@v2
#
#      - name: build
#        run: ./build.sh --config RelWithDebInfo --build_shared_lib --parallel --cmake_extra_defines CMAKE_OSX_ARCHITECTURES=x86_64
#  linux_release:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Check out a copy of the repository
#        uses: actions/checkout@v2
#
#      - name: build
#        run: ./build.sh --config RelWithDebInfo --build_shared_lib --parallel
  windows_release:
    runs-on: windows-2019
    steps:
      - name: Check out a copy of the repository
        uses: actions/checkout@v2
      - uses: seanmiddleditch/gha-setup-vsdevenv@master
      - name: build
        run: .\build.bat --config RelWithDebInfo --build_shared_lib --parallel